import pyodbc as odbc

def conn():
    conn = odbc.connect('Driver = { ----};' #sensitive data, only enter on PFG side
                        'Server = ----;' #unsure what server name/data base name are. ----?
                        'Database = ----;'
                        'Trusted_Connection = yes;')
