import pandas as pd
import numpy as np
import Orig_Sub as os
import Final_Sub as fs
import User_Contract as uc

contractPrefix = uc.userContractPrefixValidation(input("Please provide the contract prefix: "))
contract5Digit = uc.userContractNumberValidation(input("Please provide the 5 digit contract number: "))
subMerge = ["Contract","Submission ID","Contribution Type","Member ID","SSN","Pay Period",
            "Effective Date","Submission Date"]
dfCompare = pd.merge(os,fs,on=subMerge,how='outer')
#x table = original. y table = final. z table = recordkeeping.




